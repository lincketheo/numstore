version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: numstore-docs
    ports:
      - "8080:80"
    restart: unless-stopped
    # Uncomment below for custom nginx config override
    # volumes:
    #   - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
