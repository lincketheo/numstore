file(GLOB NSRPTCURSOR_SRC CONFIGURE_DEPENDS *.c)
file(GLOB NSRPTCURSOR_TEST_SRC CONFIGURE_DEPENDS "testing/*.c")

add_ns_library(nsrptcursor
  SOURCES
    ${NSRPTCURSOR_SRC}
  TEST_SOURCES
    ${NSRPTCURSOR_TEST_SRC}
  DEPENDENCIES
    ${LIBS}/nspager
)
