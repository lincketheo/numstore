

insert into db ds signal 
insert into db ds match
insert into db metric foo 

create transform caf_transform (t: size_t, f: float);

void foo(complex float* l, size_t llen, complex float* r, size_t rlen) {
  float* caf;
  size_t cafl; // 
  // run caf 

  for(int i 
}
